import styled from 'styled-components';

export default styled.div`
  display: flex;
  justify-content: space-between;
  margin-bottom: 50px;

  .filters--title {
    font-size: 40px;
    line-height: 60px;
    margin: 0;
  }

  .filters--book-filter {
    display: flex;
    gap: 20px;
  }
`;
